// Escreva uma função que recebe 2 números e retorne o maior deles

//maximo
const max = (x, y) =>  x > y ? x : y;  
console.log(max(80, 70));
//mínimo 
const min = (x,y) => x < y ? x: y;
console.log(min(21, 46))
